# FoodApp (mobile version)

## Get started

0. Clone the repository
1. Install the dependencies
   - In the /client directory, run `npm install`

## Development

The frontend is developed using React.js and Typescript in the /client directory.